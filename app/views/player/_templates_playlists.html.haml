%script#playlist-tab-template{ :type => "text/template" }
  %a{ :href => "#playlist<%- id %>".html_safe, :"data-toggle" => "tab", :title => "<%- description %>".html_safe, :rel => "tooltip", :"data-placement" => "bottom" }
    <%- title %>
  
%script#playlist-container-template{ :type => "text/template" }
  %table
    %tbody.playlist-dragndrop
      %tr.message-empty.hide
        %td{ :colspan => 4 }
          .text
            %p
              This playlist is empty. 
            %p
              Use search to find tracks and drag'n'drop them here.
