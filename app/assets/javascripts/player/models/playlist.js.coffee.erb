class CloudAmp.Models.Playlist extends Backbone.Model
  url: "<%= JS.escape(Rails.application.routes.url_helpers.playlists_path) %>"
  
  initialize: ->
    @tracks = new CloudAmp.Models.TrackCollection
    
  teardown: ->
    @tracks = null
    
